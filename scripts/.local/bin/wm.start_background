#!/usr/bin/env bash

# Hide plasma shell
killall -q plasmashell

# Wait for plasmashell to close
while pgrep -u $UID -x plasmashell >/dev/null; do sleep 1; done

# Set background
~/.fehbg

# fix cursor icon
xsetroot -cursor_name left_ptr
